<% if @merchant %>
  <h1><%= link_to @merchant.name, "/merchants/#{@merchant.id}"%><span> Items</span></h1>
  <p align="center"><%= link_to "Add New Item", "/merchants/#{@merchant.id}/items/new" %></p>
<% else %>
  <h1>All Items</h1>
<% end %>
<section>
  <h3>Statistics</h3>
    Best Sellers:
    <% x = 1 %>
    <% @items.top_five_items.each do |item, quantity| %>
    <li><%= x %>. <%= item.name %>  Quantity: <%= quantity %></li>
    <% x +=1 %>
    <% end %>
    Worst Sellers:
    <% x = 1 %>
    <% @items.bottom_five_items.each do |item, quantity| %>
    <li><%= x %>. <%= item.name %>  Quantity: <%= quantity %></li>
    <% x +=1 %>
    <% end %>
</section>
<section class="grid-container">
  <% @items.each do |item| %>
    <section class = "grid-item" id= 'item-<%=item.id%>'>
      <h2> <%=link_to item.name, "/items/#{item.id}" %> </h2>
      <p>Sold by: <%=link_to item.merchant.name, "/merchants/#{item.merchant.id}" %></p>
      <a href=<%= "/items/#{item.id}" %>> <img alt="img" src= <%= item.image %>></a>
      <p> <%= item.description unless @merchant%> </p>
      <p>Price: <%=number_to_currency(item.price) %> </p>
      <p>Inventory: <%= item.inventory %> </p>
      <% if !@merchant %>
      <% end %>
      <% if item.active? %>
        <p>Active</p>
      <% else %>
        <p>Inactive</p>
      <% end %>
    </section>
    <% end %>
</section>
